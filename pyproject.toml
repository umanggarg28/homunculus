[project]
name = "homunculus"
version = "0.1.0"
description = "Minimal autonomous personal AI assistant — built from scratch, no frameworks."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "python-telegram-bot>=21",
]
